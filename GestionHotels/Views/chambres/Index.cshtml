@model IEnumerable<GestionHotels.Models.chambre>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.etat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Hotel.adresse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.reservation.idRes)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.etat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Hotel.adresse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.reservation.idRes)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.numChamb }) |
            @Html.ActionLink("Details", "Details", new { id=item.numChamb }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.numChamb })
        </td>
    </tr>
}

</table>
