@model IEnumerable<GestionHotels.Models.reservation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.dateRes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateDebutRes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dateFinRes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Client.nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dateRes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateDebutRes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dateFinRes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client.nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRes }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRes }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRes })
        </td>
    </tr>
}

</table>
