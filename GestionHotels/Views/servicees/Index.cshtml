@model IEnumerable<GestionHotels.Models.servicee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descriptions)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prix)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descriptions)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prix)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSer }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSer })
        </td>
    </tr>
}

</table>
